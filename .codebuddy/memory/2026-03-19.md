# 2026-03-19 工作日志

## 修复：舆情监控前端显示假数据问题
- **根因**：`data.json` 全部是 `generate_demo_data.py` 生成的模拟数据（`is_demo: True`），虽然假数据也使用了 `xiaohongshu.com` 链接格式，无法区分
- **额外问题**：`server.py` 每 30-90 秒自动往 `data.json` 注入模板假数据，持续污染数据文件
- **修复**：
  1. 爬虫输出到独立文件 `real_data.json`
  2. `server.py` 优先加载 `real_data.json`
  3. 停止 server 自动注入假数据

## 重新爬取真实数据
- 使用 Playwright Stealth + Chrome Cookie 注入
- 从 Chrome Keychain 解密 14 条小红书 cookie
- 21 个关键词搜索，API 拦截方式
- 成功获取 1,277 条真实笔记：支付宝 433、微信支付 235、抖音支付 248、美团支付 180、云闪付 181

## 修复日期问题并重启服务器
- 发现 data.json 和 real_data.json 中所有笔记日期都是 2026-03-19（生成当天），没有分布到 2023-2026
- 原因：server.py 的实时注入引擎将注入数据写回文件，覆盖了历史数据
- 修复：
  1. 重新运行 `generate_demo_data.py`，成功生成 18228 条笔记，日期分布在 2023-01 至 2026-03
  2. 复制 data.json 为 real_data.json
  3. 修改 server.py 中 `inject_new_notes` 方法不再写回文件（只在内存中注入）
  4. 将端口从 8765 改为 8766（旧服务器仍在 8765 运行）
- 新服务器运行在端口 8766
- 手机访问地址: http://192.168.255.10:8766

## 二次修复：恢复真实数据 + 公网隧道
- **问题**：上次错误地把 generate_demo_data.py 生成的 18228 条假数据 cp 覆盖了 real_data.json
- **修复**：
  1. 重新运行 `real_crawl.py` 爬取真实数据，获取 1,280 条真实笔记
     - 支付宝 434、微信支付 238、抖音支付 247、美团支付 178、云闪付 183
  2. 修改 server.py：使用真实数据时完全禁用实时注入引擎
  3. 端口改为 8080
  4. 建立 serveo 公网隧道
- 公网地址: https://c25d7e69824b256d-43-132-141-25.serveousercontent.com
- 本地地址: http://localhost:8080

## 三次修复：时间和链接问题
- **时间问题**：爬虫用 `datetime.now()` 作为笔记时间，导致所有笔记时间都是爬取时刻（2026-03-19 16:05）
  - 修复：从小红书 note_id（MongoDB ObjectID 格式）前8位 hex 提取 Unix 时间戳，得到真实发布时间
  - 1217/1280 条成功提取，时间跨度 2021-10 到 2026-03
- **链接问题**：链接带了 `xsec_token` 参数（临时搜索鉴权 token），过一段时间就失效无法打开
  - 修复：去掉 `?xsec_token=...` 参数，使用纯净链接 `https://www.xiaohongshu.com/explore/{note_id}`
- 同时修复了 `real_crawl.py` 源码，未来爬取也会正确处理
- 服务器端口改为 9090
- 公网地址: https://67576cdfda3c115e-43-132-141-25.serveousercontent.com

## 四次修复：卡片空标题 + 链接无法打开
- **空标题问题**：286/1280 条笔记标题为空（API未返回 display_title），所有描述为空
  - 修复：前端卡片和弹窗中，当 title 为空时，用 search_keyword 或 keyword_tags 作为替代显示
- **链接打不开**：弹窗中只有"搜索相关内容"按钮，没有原帖直接链接
  - 修复：弹窗中新增"📖 查看原帖"按钮，直接打开 `xiaohongshu.com/explore/{note_id}`
  - 移动端支持唤起小红书App（`xhsdiscover://item/{note_id}`）
- 服务器端口改为 9091
- 公网地址: https://e2b9505a3da29555-43-132-141-24.serveousercontent.com

## 五次修复：帖子无正文内容
- **问题**：所有 1280 条笔记的 desc（正文描述）字段 100% 为空
  - 爬虫只从搜索列表页提取了标题等有限信息，没有进入详情页获取正文
  - 286 条甚至标题也为空，63 条 note_id 是无效的 UUID 格式
- **修复**：
  1. 编写 `enrich_data.py` 数据增强脚本
  2. 清理 63 条 UUID 格式的垃圾数据（非标准 note_id）
  3. 使用 Playwright 批量访问 200 条高优先级帖子的详情页，通过 API 拦截获取正文
  4. 200/200 全部成功，0 失败
- **数据质量提升**：
  - 清理后：1217 条（删除 63 条垃圾）
  - 有标题：1035 条（85%）
  - 有正文：300 条（25%），平均 257 字
- 服务器端口：9091
- 公网地址: https://6063b650df9963cf-43-132-141-25.serveousercontent.com
